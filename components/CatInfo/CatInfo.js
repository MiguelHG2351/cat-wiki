// import { useBreedInfoData } from 'hooks/useBreedInfo'

export default function CatInfo_({ ids }) {
    // const { data, isLoading } = useBreedInfoData(ids)
    return (
        <div>
            <div>{ ids }</div>
        </div>
    )
}
